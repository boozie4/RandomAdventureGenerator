// mobile-config.js
// This file is intended to be edited per-platform (or replaced during build).
// Define API_BASE when running inside a device/emulator to point to your backend.

// Examples (uncomment the one that applies):
// For Android emulator (default Android Emulator):
// window.API_BASE = 'http://10.0.2.2:3000';

// For Android with Genymotion: replace with host IP
// window.API_BASE = 'http://<HOST_IP>:3000';

// For iOS Simulator (localhost works):
// window.API_BASE = 'http://localhost:3000';

// For physical devices use host machine IP (e.g. 192.168.1.100)
// window.API_BASE = 'http://192.168.1.100:3000';

// Default: undefined (scripts.js will fall back to relative paths)
